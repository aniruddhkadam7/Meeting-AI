# Torch-free subset of requirements.txt, used only to build the frozen
# "rag-lite" service bundled into desktop release installers (see
# packages/rag/scripts/freeze_rag_lite.py). Deliberately excludes
# sentence-transformers (and the ~800MB of torch/transformers/etc. it pulls
# in) — that's what makes semantic search/embedding unavailable in this
# build; text extraction (pypdf/python-docx) and everything else in the
# service works exactly the same either way. See
# app/pipeline.py's ImportError handling in process_document.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-multipart>=0.0.9,<1.0

pypdf>=6.0,<7.0
python-docx>=1.1,<2.0
sqlite-vec>=0.1.9,<0.2
